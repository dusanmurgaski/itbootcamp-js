function generateList(parent){
    let lista = document.createElement("ul");
    lista.appendChild(`${parent}`);
}

function generateListItem(parent,src){
    let items = document.createElement("li");
    items.appendChild(`${parent}`);
    items.document.
}